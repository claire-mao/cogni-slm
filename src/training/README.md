# Training Source Namespace

This folder is reserved for reusable training library code under `src/`.

Current training runtime modules remain in top-level `training/` to preserve the existing CLI (`python -m training.run`).
When training internals are stabilized, shared logic can be migrated here incrementally.
